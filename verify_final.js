const { getAnimeDetails, getEpisodeServers } = require('./lib/hianime'); async function run() { const animeId = 'jujutsu-kaisen-the-culling-game-part-1-20401'; console.log('Checking ID:', animeId); const anime = await getAnimeDetails(animeId); if(anime) { const epId = anime.episodeList[0].id; console.log('Checking Ep:', epId); const servers = await getEpisodeServers(epId); console.log(servers); } else { console.log('Anime not found'); } } run();
