import { searchAnimeFromSite, getAnimeDetails, getEpisodeServers } from './lib/hianime'; async function run() { const search = await searchAnimeFromSite('Naruto'); if (search.length > 0) { const anime = await getAnimeDetails(search[0].id); if (anime && anime.episodeList.length > 0) { const epId = anime.episodeList[0].id; console.log('Checking ID:', epId); const servers = await getEpisodeServers(epId); console.log(servers); } } } run();
